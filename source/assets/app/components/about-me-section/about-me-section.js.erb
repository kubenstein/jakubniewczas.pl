angular.module('jakubniewczas')
  .directive('aboutMeSection', function() {
    return {
      scope: {
        type: '@'
      },
      template: '<ng-include src="getTemplateUrl()" />',

      controller: ['$scope', function($scope) {
        $scope.getTemplateUrl = function() {
          if($scope.type === 'blog') {
            return '<%= ng_template_url("about-me-section/blog-about-me-section") %>';
          }

          if($scope.type === 'twitter') {
            return '<%= ng_template_url("about-me-section/twitter-about-me-section") %>';
          }

          return '<%= ng_template_url("about-me-section/") %>';
        }
      }]
    }
  })