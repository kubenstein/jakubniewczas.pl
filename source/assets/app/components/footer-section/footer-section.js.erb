angular.module('jakubniewczas')
  .directive('footerSection', function() {
    return {
      scope: {
        compact: '='
      },
      templateUrl: '<%= ng_template_url("footer-section/") %>',
      controller: ['$scope', function($scope) {
        $scope.currentYear = new Date();
      }]
    }
  })
